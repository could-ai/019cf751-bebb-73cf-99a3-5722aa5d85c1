import 'package:flutter/material.dart';

class DeviceModel {
  final String name;
  final Size size;
  final String platform;

  DeviceModel({
    required this.name,
    required this.size,
    required this.platform,
  });
}